// Interface terms, resolved as a cascade: the default language defines every
// key, the page language overrides what it translates. Later assignments win in
// AsciiDoc, so this needs no file-existence check, which AsciiDoc cannot express.
include::./i18n/cascade.adoc[]
// Page references, generated per language: same-language page where one exists,
// default language otherwise. Values are relative to {basedir}, so they work at
// any page depth and in the local and PDF builds.
include::./generated/i18n/links-{lang}.adoc[opts=optional]
:docinfo: shared
:docinfodir: {includesdir}/docinfo/
ifndef::basedir[]
:basedir: .
endif::[]
// The version the theme's stylesheets and scripts are addressed by, so a
// changed theme reaches a reader who still holds the previous one. The build
// passes the real value; a local render outside the build has no theme to
// version and says so rather than leaving the reference unresolved.
ifndef::theme_version[]
:theme_version: dev
endif::[]
:stylesheet: style.css
:stylesdir: {basedir}/../theme
:iconsdir: {stylesdir}/icons
ifndef::imagesdir[]
:imagesdir: {includesdir}/images/
endif::[]
:copycss: {basedir}/src-content/theme/style.css
:icons: image
ifdef::buildsite[]
:stylesdir: {basedir}/stylesheet
:iconsdir: {basedir}/assets/icons
// Only where the stylesheet is linked, and only with the copy switched off.
// :copycss: makes AsciiDoc write the stylesheet to a file named by this value,
// and it does so alongside :linkcss: rather than instead of it — a query string
// then lands in the file name, where `?` is legal on this machine and rejected
// by the artifact upload. The copy is redundant here anyway: the Gradle copy
// task puts the stylesheet in place and versions the import it makes.
:stylesheet: style.css?v={theme_version}
:linkcss:
:copycss!:
ifeval::["{backend}" != "pdf"]
:imagesdir: {basedir}/assets/images/
endif::[]
endif::[]
:toc-title: {ui_toc_title}
ifndef::toc[]
:toc:
endif::[]
ifeval::["{toc}" == ""]
:toc: macro
endif::[]
:nofooter:
:noheader:
ifeval::["{toc}" == "left"]
:noheader!:
endif::[]
ifeval::["{toc}" == "right"]
:noheader!:
endif::[]
ifeval::["{backend}" == "pdf"]
:icons: font
:nofooter!:
:copycss!:
:stylesdir!:
endif::[]
ifeval::["{type}" != "readme"]
ifeval::["{backend}" != "pdf"]
include::./menue.adoc[]
include::../../theme/doctitle.adoc[]
endif::[]
endif::[]
include::../../revinfo.adoc[]
